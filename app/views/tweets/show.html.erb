<!DOCTYPE html>
<html>
<head>
  <title>Clon de Twitter</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-4">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <p class="text-success"><%= notice %></p>

      <!-- Detalles del tweet -->
      <div class="card">
        <div class="card-header">
          Tweet
        </div>
        <div class="card-body">
          <%= render @tweet %>
        </div>
      </div>

      <!-- Enlaces de edición y eliminación -->
      <div class="mt-3">
        <%= link_to "Edit this tweet", edit_tweet_path(@tweet), class: "btn btn-sm btn-outline-primary" %> |
        <%= link_to "Back to tweets", tweets_path, class: "btn btn-sm btn-outline-secondary" %>

        <%= button_to "Destroy this tweet", @tweet, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-outline-danger", style: "margin-left: 10px;" %>
      </div>
    </div>
  </div>
</div>

</body>
</html>
